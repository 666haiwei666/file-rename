const clearQuestionList = [
    {
        type: 'confirm',
        name: 'confirm',
        message: 'Are you sure you want to delete the cache file(是否确认删除缓存文件)',
        default: true
    }
]

module.exports = {
    clearQuestionList
}